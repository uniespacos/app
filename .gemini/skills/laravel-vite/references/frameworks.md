# Frameworks

## React
- **Plugin**: `@vitejs/plugin-react`
- **Version**: React 18
- **TypeScript**: Enabled (`tsconfig.json` with `jsx: react-jsx`)

## Configuration
```typescript
import react from '@vitejs/plugin-react';

plugins: [
    react(),
]
```
