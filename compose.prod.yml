services:
    web:
        container_name: web-production
        image: ghcr.io/${GHCR_REPO_WEB:-uniespacos/app/web}:${IMAGE_TAG:-latest}
        restart: unless-stopped
        env_file:
            - .env
        ports:
            - "80:80"
            - "443:443"
            - "443:443/udp"
        volumes:
            - uniespacos-storage-production:/var/www/storage:ro
            - ${PATH_TO_PRODUCTION_ENVIRONMENT_FOLDER}/Caddyfile:/etc/caddy/Caddyfile:ro
            - ${PATH_TO_PRODUCTION_ENVIRONMENT_FOLDER}/certs/uesb2025fullchain.pem:/etc/caddy/certs/uesb2025fullchain.pem:ro
            - ${PATH_TO_PRODUCTION_ENVIRONMENT_FOLDER}/certs/uesb2025privkey.pem:/etc/caddy/certs/uesb2025privkey.pem:ro
            - caddy_data_production:/data
            - caddy_config_production:/config
        networks:
            - uniespacos-production
        depends_on:
            - app
    app:
        container_name: app-production
        image: ghcr.io/${GHCR_REPO_APP:-uniespacos/app/app}:${IMAGE_TAG:-latest}
        restart: unless-stopped
        volumes:
            - uniespacos-storage-production:/var/www/storage
        env_file:
            - .env
        networks:
            - uniespacos-production
        depends_on:
            postgres:
                condition: service_healthy
    queue-worker:
        container_name: queue-worker-production
        image: ghcr.io/${GHCR_REPO_APP:-uniespacos/app/app}:${IMAGE_TAG:-latest}
        restart: unless-stopped
        command: php artisan queue:work --verbose --tries=3 --timeout=90
        volumes:
            - uniespacos-storage-production:/var/www/storage
        env_file:
            - .env
        networks:
            - uniespacos-production
        depends_on:
            - postgres
            - app
    reverb:
        container_name: reverb-production
        image: ghcr.io/${GHCR_REPO_APP:-uniespacos/app/app}:${IMAGE_TAG:-latest}
        restart: unless-stopped
        command: php artisan reverb:start --host=0.0.0.0 --port=9000
        env_file:
            - .env
        networks:
            - uniespacos-production
        depends_on:
            - postgres
            - app

    postgres:
        container_name: postgres-production
        image: postgres:16
        restart: unless-stopped
        user: postgres
        env_file:
            - .env
        environment:
            - PGPASSWORD=${DB_PASSWORD}
            - POSTGRES_DB=${DB_DATABASE}
            - POSTGRES_USER=${DB_USERNAME}
            - POSTGRES_PASSWORD=${DB_PASSWORD}
        volumes:
            - 'postgres-data-production:/var/lib/postgresql/data'
        networks:
            - uniespacos-production
        healthcheck:
            test: [ "CMD-SHELL", "pg_isready -U ${DB_USERNAME} -d ${DB_DATABASE}" ]
            interval: 10s
            timeout: 5s
            retries: 5
            start_period: 60s

networks:
    uniespacos-production:


volumes:
    postgres-data-production:
    uniespacos-storage-production:
    caddy_data_production:
    caddy_config_production:
