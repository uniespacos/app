<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- force="true" pelo mesmo motivo dos demais abaixo: o container de
             desenvolvimento exporta APP_ENV=development via .env, entao sem ele
             a suite rodava com env "development". Isso silenciava os guards de
             App::environment('testing') — o bypass da pagina de erro Inertia
             nunca era exercitado como documentado, e runningUnitTests() era
             false. Ver issue #112. -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_URL" value="http://localhost"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <!-- force="true" e obrigatorio em tudo que o container tambem exporta:
             sem ele o PHPUnit mantem o valor do ambiente e a configuracao daqui
             e ignorada em silencio. Sem force, DB_* fazia a suite rodar (e
             apagar, via RefreshDatabase) o banco de desenvolvimento; CACHE_STORE
             caia no driver `database`; e QUEUE_CONNECTION enfileirava os jobs na
             tabela `jobs` em vez de executa-los, entao testes que dependem do
             efeito de um job passavam sem o job ter rodado. -->
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="pgsql" force="true"/>
        <env name="DB_DATABASE" value="uniespacos_test" force="true"/>
        <env name="DB_USERNAME" value="uniespacos" force="true"/>
        <env name="DB_PASSWORD" value="uniespacossecretkey" force="true"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
